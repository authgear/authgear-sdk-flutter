name: flutter_authgear_example
description: Demonstrates how to use flutter_authgear

publish_to: 'none'

environment:
  sdk: ^3.0.0
  flutter: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  flutter_authgear:
    path: ../
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
